{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "superplane-kubernetes-exporter.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "superplane-kubernetes-exporter.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
